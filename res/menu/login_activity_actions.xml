<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android" 
	  xmlns:tartanizeme="http://schemas.android.com/apk/res-auto" >
    
    <!-- Labels, should appear as action button -->
    <item android:id="@+id/action_labels"
          android:icon="@drawable/ic_action_tag"
          android:title="Labels"
          tartanizeme:showAsAction="ifRoom" />
    
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_search"
          android:icon="@drawable/ic_action_search"
          android:title="Search"
          tartanizeme:showAsAction="ifRoom" />
    
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_settings"
          android:title="@string/action_settings"
          android:showAsAction="never" />

</menu>
